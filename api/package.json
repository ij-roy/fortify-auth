{
  "name": "fortify-auth-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start:prod": "node dist/server.js",
    "test": "npm run build && node --test dist/**/*.spec.js",
    "test:dev": "npm run build && node --test --watch dist/**/*.spec.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3"
  }
}
